import React from "react";

const Testimonal = ({ image }) => {
  return (
    <div className="thumb">
      <img src={image} alt="testimonial-customer" />
    </div>
  );
};

export default Testimonal;
